# Investment Research Project Plan

## 目的

このリポジトリは、少額の実運用とPythonによる投資リサーチを組み合わせ、以下のループを作ることを目的とする。

> 実際の投資 → 市場データ収集 → 分析 → 仮説 → バックテスト → 検証 → 投資判断の材料へ反映

実際の資金は小さく抑え、研究対象は広くする。  
研究結果をそのまま自動売買へ接続するのではなく、まず「正しくデータを扱い、再現可能な分析を行う」ことを重視する。

---

## Phase 0 — 実運用環境・ルールの確定

### 目的
実際に投資を開始するための環境とルールを決める。

### やること
- 証券口座・NISA等の利用環境を確認
- 毎月の投資額を決定
- 投資対象・積立方法を決定
- 手数料・税制・約定ルールを確認
- 実運用と研究用シミュレーションを区別
- 投資判断を記録する方法を決める

### 現在の状態
**進行中**

- 毎月の投資額：**10,000円で決定**
- 実運用と研究用シミュレーションを分離する方式：Phase 9で実装済み
- 投資判断・約定・入出金・配当・手数料・税の記録方式：Phase 9で実装済み
- 残り：**証券口座・口座区分（NISA / 特定口座）・実際に積み立てる資産/戦略・注文方式（整数株/端数株）・注文上限の確定**

### 完了条件
- 実際にいくら・何へ・どの頻度で投資するか決まっている
- 実運用と研究環境が混同されていない
- 投資判断を後から検証できる記録方法がある

---

## Phase 1 — 市場データ基盤

### 目的
投資分析に使える市場データを取得・検証し、再現可能な基本分析パイプラインを作る。

### やること
- yfinanceによる市場データ取得
- CSVへの保存
- 調整後価格・配当の扱いを明確化
- 欠損・重複・異常値のチェック
- 日次リターンの計算
- 年次・月次リターンの計算
- CAGRの計算
- 年率ボラティリティの計算
- 最大ドローダウンの計算
- 基本グラフの作成
- GitHubで結果を保存

### 現在の状態
**完了**

VOOについて、データ取得・品質チェック・基本的なリターン/リスク分析まで実装済み。

---

## Phase 1.5 — データ定義・品質の最終確認

### 目的
Phase 2以降で複数資産を扱う前に、データの意味と品質基準を固定する。

### やること
- adjusted price / total returnの定義を確認
- データ取得条件をコード上で明示
- 市場休日を欠損として誤検出しないことを確認
- データ取得期間・タイムゾーンを確認
- 再取得時のデータ差分について記録
- READMEにデータ仕様を記載
- VOOを基準データセットとして固定

### 完了条件
- 「このCSVの各列が何を表すか」を説明できる
- 同じコードを再実行して同じ種類のデータを取得できる
- データ品質チェックの基準が決まっている

---

## Phase 2 — 複数資産の比較分析

### 目的
VOO単体から、複数の資産・指数・ETFを同じ方法で比較できるようにする。

### 候補
- VOO：米国大型株 / S&P 500
- VT：全世界株式
- VTI：米国株式全体
- 日本株を代表するETF・指数
- 現金・債券系資産
- 必要に応じてその他の資産

### やること
- 共通データ取得処理を作る
- ティッカーを設定ファイル等で管理
- 同一期間に揃える
- リターンを比較
- CAGRを比較
- ボラティリティを比較
- 最大ドローダウンを比較
- ローリングリターン・ローリングボラティリティを分析
- 相関係数・相関行列を分析
- 同じグラフ・CSV形式で比較結果を出力

### 完了条件
「どの資産が一番良いか」を決めるのではなく、**資産ごとのリターン・リスク・相関の違いを定量的に比較できる**。

---

## Phase 3 — 積立投資シミュレーション

### 現在の状態
**完了**

### 実装済み
- 月10,000の固定額を各月最初の取引日に投資
- 小数単位の購入
- 同じ総投資額を初日に一括投資するベースライン
- 累積拠出額・評価額・損益を記録
- 資産ごとの結果をCSVへ出力

### 重要な前提
現在の価格データはUSD建てのため、10,000は正規化した拠出額として扱う。円/USD為替、手数料、税、実際の約定条件はPhase 9で扱う。

### 目的
「毎月一定額を投資する」という実運用に近い条件で、資産形成の挙動を分析する。

### やること
- 月10,000円などの定額積立をモデル化
- 一括投資との比較
- 積立期間を変更
- 投資開始時期を変更
- 暴落直前・暴落中・回復期など開始時点別に比較
- 累積投資額と評価額を記録
- 含み損期間・回復期間を分析
- DCAの結果をグラフ化

### 完了条件
「一定額を一定間隔で投資した場合」の資産推移を、過去データから再現できる。

### 完了内容
- 開始月をずらしたローリングシナリオを実装
- 1 / 3 / 5 / 10年の投資期間を比較
- 年初開始シナリオを比較
- 15%以上の価格下落を暴落エピソードとして抽出
- 暴落の高値・底・回復時点からの積立/一括投資を比較
- 積立に入金を加味した最大ドローダウンと回復期間を計算
- 開始日ごとの結果分布をCSVに出力
- DCAと一括投資の比較グラフを出力
- DCAエンジンとシナリオ分析の自動テストを追加

---

## Phase 4 — ポートフォリオ分析

### 目的
複数資産を組み合わせた場合のリスク・リターンを分析する。

### やること
- 資産配分を設定できるようにする
- 例：株式100%、株式/債券など
- ポートフォリオリターン計算
- ポートフォリオボラティリティ計算
- 最大ドローダウン計算
- 相関による分散効果を確認
- リバランスの有無を比較
- 定期リバランスのシミュレーション
- Sharpe ratio等の指標を必要に応じて追加

### 完了条件
複数資産の組み合わせと配分を変えたとき、リスク・リターンがどう変わるか定量的に比較できる。

### 現在の状態
**完了**

### 完了内容
- 複数資産の配分を設定ファイルで管理
- ウェイト・ティッカー・リバランス設定の自動検証
- Buy & Hold / 年次 / 四半期 / 月次リバランスを実装
- 一括投資と月次積立を同一ポートフォリオエンジンで比較
- ポートフォリオの日次リターン・NAV・CAGR・ボラティリティ・最大ドローダウンを計算
- BILを無リスク資産としたSharpe ratioを計算
- 共分散行列による分散効果を計算
- 複数ポートフォリオを同一期間・同一条件で比較
- CSVレポートとグラフを出力
- ポートフォリオ専用テスト32件を追加（テスト全体56件）
- Phase 3のDCAエンジンとの整合性をテスト

実装済みの代表的な比較は、6ポートフォリオ × 4リバランス頻度 × 一括/積立の48通り。

---

## Phase 5 — バックテスト基盤

### 目的
投資戦略を「ルール」として記述し、過去データ上で再現できるようにする。

### やること
- Strategyの共通インターフェースを作る
- 売買ルールをコード化
- ポジション管理
- キャッシュ管理
- 手数料・スリッページを考慮
- 売買履歴を保存
- ポートフォリオ価値を計算
- ベンチマークとの比較
- 期間別・市場環境別の検証
- look-ahead bias等のバックテスト上の問題を防ぐ

### 完了条件
同じ入力データ・同じ戦略なら同じ結果を再現でき、売買ルールと仮定を説明できる。

---

### 現在の状態
**完了**

### 完了内容
- Strategy 共通インターフェース
- Buy & Hold / 定期リバランス / 移動平均 / モメンタムの4戦略
- ポジション・現金・資産評価額の管理
- 翌営業日終値での約定
- 売買履歴・シグナルの保存
- 手数料・最低手数料・スリッページ
- 売買資金不足時の買い注文スケーリング
- look-ahead biasを避けるための履歴制限
- ベンチマーク比較
- 年次リターン・暴落/回復期などの市場環境別レポート
- run_info.json によるデータ・設定ハッシュと実行環境の記録
  - 実行時の未コミット変更の有無（git_dirty）も記録
  - ハッシュは改行コードをLFにそろえて計算し、OSに関係なくgit上の内容と一致する
- Phase 5専用テスト26件（テスト全体82件）
- GitHub Actionsによるpytest自動実行設定

### 最終検証の結果（2026-09-23）
1. GitHub Actions：全テスト成功（Linux / Python 3.11）
2. 未コミット変更のない状態（commit 14f285d）でバックテストレポートを再生成。数値のCSV 6本とグラフは再生成前と1バイトも変わらなかった
3. run_info.json：git_commit が実行時のHEADと一致し、git_dirty は false。データ・設定のハッシュは、そのcommitに保存された内容から計算した値と一致
4. READMEの結果表（成績31か所・市場環境別24か所の計55か所）が再生成レポートと一致

検証中に見つかり修正した問題：
- run_info.json が過去のcommitを参照していた → 未コミット変更の有無も記録し、変更のない状態で再生成
- Windowsで保存したCSV（CRLF）のハッシュが、git上の内容（LF）と一致しなかった → 改行コードをそろえて計算するよう修正

---

## Phase 6 — ポートフォリオ数学・統計

### 目的
バックテスト結果を単純な損益だけでなく、統計的・定量的に評価する。

### やること
- CAGR
- 年率ボラティリティ
- 最大ドローダウン
- Sharpe ratio
- Sortino ratio
- Calmar ratio
- VaR / CVaR等の必要なリスク指標
- 勝率・平均損益
- リターン分布
- ローリング指標
- 期間別・市場局面別の分析
- 統計的有意性や過学習への注意

### 完了条件
「利益が出た」という結果だけでなく、**どのようなリスクを取ってその結果になったのか**を分析できる。

### 現在の状態
**完了**

### 完了内容
- Phase 5の4戦略を同じ条件で再実行し、全戦略に同じ定義の指標を適用
- 基本指標：累積リターン・CAGR・年率ボラティリティ・最大ドローダウン（高値・底・回復日）・回復期間・最終資産
- リスク調整指標：Sharpe・Sortino・Calmar（BILを無リスク金利、定義できない場合はNaN）
- リターン分布：平均・中央値・標準偏差・歪度・尖度・最小・最大・7つのパーセンタイル（日次・月次）
- ヒストリカルVaR / CVaR（95%・99%、損失を正の数で表す、件数不足時はNaN）
- ローリング指標：リターン・ボラティリティ・Sharpe・窓内の最大DD・ベンチマークとの差（63日・252日）
- 売買単位の損益：勝率・平均利益/損失・Profit Factor・最大連勝/連敗（適用できない戦略は理由を記録）
- Buy & Hold VOOとの差を8指標で記録（優劣の判定はしない）
- 戦略・VOO・BILの日次・月次・下落日の相関と共分散
- 市場局面（強気・弱気・高ボラ・低ボラ・大幅下落）を過去データだけで定義し、局面別の成績を集計
- 統計的検証：平均リターンの信頼区間（通常・Newey-West）、Sharpeの標準誤差（Lo・Mertens）、Probabilistic Sharpe Ratio、seed固定のブートストラップ（1日単位・21日ブロック）、Holm補正
- バックテストの統計的な限界（look-ahead・生き残り・選択・データののぞき見・多重検定・過学習）とPhase 7の注意点をREADMEに記載
- Phase 6専用テスト34件（テスト全体116件）
- 入力ハッシュ・commit・git_dirty・バージョンをrun_info.jsonに記録（Phase 5と共通の仕組み）

### 最終検証の結果（2026-09-23）
1. GitHub Actions：全テスト116件が成功（Linux / Python 3.11）
2. 未コミット変更のない状態（commit 1c2bc0b）でレポートを再生成。CSV 12本は再生成前と1バイトも変わらなかった
3. run_info.json：git_commit が実行時のHEADと一致し、git_dirty は false。価格データ・backtest.toml・statistics.toml のハッシュが、そのcommitに保存された内容と一致
4. READMEの結果表（99か所）が再生成レポートと一致

### 分かったこと（要点）
- 15年分の日次データでも、Sharpeの標準誤差は約0.26あり、推定の幅は大きい。Buy & Holdと60/40のSharpeの差は統計的に区別できない
- CAGRは3戦略ともBuy & Holdより低いと言えるが、Sharpeで低いと言えるのはモメンタムだけ（トレンドは多重検定の補正後に区別できない）
- リターンの分布は正規分布より裾が厚く、99% VaRは正規分布を仮定した値より22〜33%大きい
- トレンド戦略は、弱気・高ボラ局面に含まれる急な反発を取り逃がしていた

---

## Phase 7 — 機械学習

### 目的
機械学習を投資研究の補助として利用し、単純なルールでは捉えにくいパターンを検証する。

### やること
- 特徴量設計
- 時系列データのtrain/validation/test分割
- ベースラインモデル作成
- 回帰・分類モデルの比較
- 時系列クロスバリデーション
- データリーク防止
- 過学習対策
- ベースライン戦略との比較
- アウト・オブ・サンプル検証

### 注意
株価予測精度だけを目的にしない。  
「予測精度が高い」ことと「投資戦略として利益・リスク面で有効」であることは別問題として扱う。

### 完了条件
機械学習モデルが、単純なベースラインを本当に上回っているかを、未使用データで検証できる。

### 現在の状態
**完了**

### 完了内容
- 予測対象：VOOの20営業日後のリターンがプラスか（y=1）。ラベルが確定しない末尾はNaNのまま学習・評価から除外
- 特徴量：過去リターン（1〜252日）・ボラティリティ・200日移動平均乖離・出来高変化（base 10個）に、Phase 6のドローダウン・ローリングSharpe・市場局面フラグと他資産との相対リターンを加えたextended 19個。すべて当日終値までのデータだけで計算し、欠損は補完せず除外
- 期間分割：train 2011〜2019年、validation 2020〜2022年、test 2023〜2026年（時系列順・重複なし）。加えて2016〜2026年の各年を前年末までのデータで予測するwalk-forward
- リーク防止：境界の手前20行のラベルを除外（purge）、標準化と特徴量選択はPipeline内で学習行だけでfit、ハイパーパラメータはvalidationのROC-AUCで選び、testは1回だけ評価、しきい値は0.5に固定
- モデル：LogisticRegression・RandomForest・HistGradientBoosting × 2特徴量セット（候補22個）。ベースライン：常に上昇（Buy & Hold）・学習期間の上昇率・モメンタム・移動平均・ランダム
- 予測指標：正解率・適合率・再現率・F1・ROC-AUC・Brier・log loss・混同行列・キャリブレーション
- 投資成果：予測を月初の売買シグナルに変換し、Phase 5のエンジン（翌日約定・コストあり）で実行、Phase 6の指標・ブートストラップ（1日単位・21日ブロック）・Holm補正でBuy & Holdと比較
- 過学習・安定性：train/validation/test/walk-forwardのROC-AUC比較、モデルの複雑さ、seed 5通りでの標準偏差、permutation importance
- 実験ログ：reports/ml/experiments.csv（設定・特徴量・ハイパーパラメータ・入力ハッシュ・commitから作る実験ID）
- Phase 7専用テスト19件（テスト全体135件）：未来データを使わないこと、purge、分割、学習行だけでのfit、seedの再現性、翌日約定、未使用期間の予測だけで売買すること など
- 入力ハッシュ・commit・git_dirty・scikit-learnなどのバージョンをrun_info.jsonに記録（Phase 5・6と共通の仕組み）

### 最終検証の結果（2026-09-23）
1. GitHub Actions：全テスト135件が成功（Linux / Python 3.11、scikit-learn 1.9.1）
2. 未コミット変更のない状態（commit 2a8223a）でレポートを再生成。CSV 28本のうち27本は再生成前と1バイトも変わらなかった。残る experiments.csv も、commit・git_dirty とそれから作る実験IDだけが変わった
3. run_info.json：git_commit が実行時のHEADと一致し、git_dirty は false。価格データ・VOOの生データ・ml.toml・backtest.toml・statistics.toml のハッシュが、そのcommitに保存された内容と一致
4. READMEの結果表（Phase 7 139か所、Phase 6 99か所）が再生成レポートと一致

### 分かったこと（要点）
- MLを使っても、投資成果がBuy & Holdより良くなったという証拠はない。テスト期間ではどのML戦略もSharpeの差を区別できず（Holm補正後 p = 1.0）、walk-forwardでも最もよくてBuy & Holdと同程度。logistic_regression / base は有意に下回った（p = 0.027）
- walk-forwardのROC-AUC平均は0.54〜0.61で、ルールのベースライン（モメンタム0.44、移動平均0.46）より高いが、年ごとのばらつきが大きく、投資成果には結びつかなかった
- 20営業日後に上がる日が約7割あるため、正解率（約71%）は予測の良さを表さない。MLはテスト期間の判断の91〜100%でVOOを持っており、実質的にはほぼBuy & Holdだった
- trainのROC-AUC（0.62〜0.93）とvalidation（0.43〜0.49）の差が大きく、過学習がはっきり見える。200日移動平均乖離は、validationで関係が逆に働いていた
- 20日先のラベルは隣の日と重なるため、独立なサンプルは実質100件程度しかない

---

## Phase 8 — Streamlit分析ダッシュボード

### 目的
これまでの分析結果をブラウザ上で操作・確認できるようにする。

### やること
- 資産選択
- 期間選択
- リターン表示
- CAGR表示
- ボラティリティ表示
- ドローダウン表示
- 相関行列表示
- 積立シミュレーション
- ポートフォリオ分析
- バックテスト結果表示
- グラフのインタラクティブ化

### 完了条件
コードを直接編集しなくても、主要な分析条件を変更して結果を確認できる。

### 現在の状態
**完了**

### 完了内容
- `streamlit run app.py` で起動する6ページのダッシュボード（① Overview、② Asset Analysis、③ Portfolio、④ Backtest、⑤ Statistics、⑥ Machine Learning）
- 共通サイドバー：資産の選択、期間の選択（日付の範囲か「直近N年」）、分析モード（「詳細」でグラフの元データの表とCSVダウンロード）。設定は全ページで共有し、ページを切り替えても入力を保持
- 「やること」との対応：リターン・CAGR・ボラティリティ・ドローダウン・Sharpe等の表示（①②）、相関行列（②、日次/月次）、積立シミュレーションとポートフォリオ分析（③、一括/毎月積立・リバランス4種・ベンチマーク比較）、バックテスト結果（④、コスト・戦略パラメータを変更可、約定ログ）、グラフのインタラクティブ化（Plotlyのホバー・ズーム・凡例切り替え）
- 追加で、Phase 6の統計検証（⑤：Sharpeの標準誤差とPSR、ブートストラップとHolm補正、Newey-West、VaR/CVaR）と、Phase 7の機械学習（⑥：予測→バックテスト→統計検証を1画面）も画面から操作可能
- 計算はPhase 2〜7の既存モジュールを呼び出すだけで、指標を定義し直していない。計算部分（compute.py・ml_compute.py）はStreamlitに依存せず、pytestで直接テスト
- 研究の規律：⑥で検証期間以外の基準でハイパーパラメータやしきい値を変えると警告、walk-forwardは既定のハイパーパラメータに固定、戦略は252取引日の履歴がそろってから取引開始
- 入力の確認：資産未選択・期間不足・比率の合計が100%でない場合などは計算せずに理由を表示
- Phase 8専用テスト25件（テスト全体160件）：Phase 4・5・7のレポートとの数値の一致、全6ページがエラーなく表示されること、入力の確認、詳細モード
- Streamlitの利用統計は送らない設定（.streamlit/config.toml）

### 最終検証の結果（2026-09-23）
1. GitHub Actions：全テスト160件が成功（Linux / Python 3.11、streamlit・plotlyをrequirements.txtからインストール）
2. commit af0f6ee を別のフォルダにcloneし、未コミット変更のない状態で全テスト160件が成功（Windows）。そのcloneから `streamlit run app.py` で起動し、ブラウザで表示されることを確認
3. 画面上の数値が既存のレポートと一致：③ Phase 4のポートフォリオ8通り、④ Phase 5の4戦略の最終資産・Sharpe・約定回数、⑤ Phase 6のSharpeの差とHolm補正後のp値（例：モメンタム 0.012）、⑥ Phase 7のROC-AUC・テスト期間のCAGR・walk-forwardの年別ROC-AUC
4. READMEのPhase 6・7の結果表（99か所・139か所）がレポートと一致したまま

### 使う時の注意
- 画面で条件を変えて良い結果を探すこと自体が、データののぞき見（多重検定）になる。画面は理解と確認に使い、結論はPhase 5〜7の決めた手順で出す
- ⑥のHolm補正は画面に出した3件の比較が対象で、Phase 7のレポート（10件）より補正後のp値が小さくなることがある
- 金額は正規化した値（USD建て）で、実際の資産額ではない。税金と為替は含まない

---

## Phase 9 — 実運用への接続

### 目的
研究結果を実際の少額投資の振り返りに利用する。

### やること
- 実際の投資履歴を記録
- シミュレーションとの差を確認
- 手数料・税・約定価格等の現実との差を確認
- 定期的に研究結果を更新
- 投資判断の根拠と結果を記録
- 仮説と実績を比較
- 必要に応じて研究テーマを更新

### 原則
**研究結果から自動的に注文を出すことは当面行わない。**

研究と実運用を分離し、研究結果を人間が確認したうえで実際の投資判断に利用する。

### 現在の状態
**完了**（実運用に使う仕組み。実際の運用と振り返りは、ここから継続して行う）

### 完了内容
- 流れ：最新データの取得 → データ確認 → シグナル（Phase 5の戦略かPhase 7のML）→ 目標比率 → 台帳から注文数量 → リスクチェック → 人が承認・却下 → 承認した注文だけ注文キューへ → 人が証券会社で発注 → 約定を台帳に記録 → 再計算。自動発注・証券会社APIは作っていない（原則どおり）
- 「やること」との対応：
  - 実際の投資履歴を記録：取引台帳（入出金・売買・配当・手数料・税金）。保有・現金・取得原価・損益は毎回台帳から再計算し、矛盾する記録は拒否
  - シミュレーションとの差：ペーパー（シャドー）トレードと、バックテスト・ペーパー・実運用の比較（同じ期間、円建ての時間加重リターン）
  - 手数料・税・約定価格の差：執行コスト（価格差・手数料・為替スプレッド）、為替、特定口座とNISAの税金、経費率の扱い
  - 投資判断の根拠と結果を記録：追記のみの監査ログ（データ・戦略設定・コードのバージョン、シグナル、目標比率、注文、リスクチェック、判断とメモ）
  - 仮説と実績を比較：損益の要因分解（資産・配分・リバランス・為替・配当・手数料と税金・約定。合計は損益と一致）
  - 研究結果の更新：研究データ（固定）と実運用の市場データを分け、Phase 1〜8の再現性を保ったまま実運用のデータだけを更新できる
- フェイルセーフ：緊急停止、価格・為替の欠損・0以下・古さ、重複注文、1日の件数・1回の金額・比率の上限、保有を超える売り、現金不足。1つでも該当すれば止め、理由を記録。STOPの提案と緊急停止中は承認できない
- ダッシュボード：⑦ Live Portfolio、⑧ Today's Decision（承認・約定と入出金の記録・緊急停止）、⑨ Research vs Real。合成データのデモ口座で操作を試せる
- 秘密情報：実際の取引・保有・判断は data/live/、個人の設定は config/live.local.toml に置き、どちらもgit管理外（テストで確認）。テストとデモは合成データと一時フォルダだけを使う
- Phase 9専用テスト28件（テスト全体188件）

### 最終検証の結果（2026-09-24）
1. GitHub Actions：全テスト188件が成功（Linux / Python 3.11）
2. commit 111b34e を別のフォルダにcloneし、未コミット変更のない状態で全テスト188件が成功（Windows）。市場データがない状態では、提案を作らずに取得の手順を表示することを確認
3. 実際の市場データ（2026-09-21まで）で取得 → 状態確認 → 提案（リスクチェック PASS）まで動くことを確認（確認用の提案は実運用の監査ログから削除済み）
4. ブラウザで、デモ口座の承認 → 約定の記録 → 未約定が0件になる流れと、要因分解の合計が損益と一致すること（説明できない差 0.000000円）を確認
5. data/live/ にはテスト・デモの書き込みがなく、コミットにも含まれていないことを確認

### 使い始める前に決めること（Phase 0 の残り）
- config/live.local.toml に、使う戦略・口座の種類（NISAか特定口座）・注文の上限を書く。既定の戦略（Buy & Hold VOO）は研究のベンチマークで、仮の値
- 整数株しか買えない場合、VOOは1株で約11.2万円（2026-09-21）なので、月1万円では数か月に1回しか買えない。端数株を使えるか、1回の上限を1株より大きくするかを決める

---

# Phase間の依存関係

```
Phase 0
  ↓
Phase 1
  ↓
Phase 1.5
  ↓
Phase 2
  ↓
Phase 3
  ↓
Phase 4
  ↓
Phase 5
  ↓
Phase 6
  ↓
Phase 7
  ↓
Phase 8
  ↓
Phase 9
```

ただし、Phase 2〜4で得られた知見によって、Phase 1〜6のコードを改善することは想定する。

---

# 開発上の基本原則

1. **データを疑う**
   - 欠損、重複、調整方法、タイムゾーン、期間を確認する。

2. **再現可能にする**
   - 手作業ではなくコードで同じ分析を再実行できるようにする。

3. **ベースラインを置く**
   - 複雑な戦略や機械学習モデルの前に、単純な投資方法と比較する。

4. **バックテストを過信しない**
   - 過去に有効だったことと、将来有効であることを同一視しない。

5. **実運用と研究を分離する**
   - 研究コードから直接注文を出さない。

6. **複雑化を急がない**
   - 「複雑なモデルを作る」ことではなく、「投資について検証可能な知識を得る」ことを目的とする。

7. **実際の少額投資を研究対象にする**
   - 実運用で得られる経験と、Pythonによる定量分析を相互に利用する。

---

# 現在地

- Phase 0：実運用環境の確定を進行
- Phase 1：**完了**
- Phase 1.5：**完了**
- Phase 2：**完了**
- Phase 3：**完了**
- Phase 4：**完了**
- Phase 5：**完了**
- Phase 6：**完了**
- Phase 7：**完了**
- Phase 8：**完了**
- Phase 9：**完了**（実運用の仕組み）

Phase 9までで、研究（Phase 1〜8）と実運用をつなぐ仕組みができた。システムは提案とリスクチェックまでを行い、承認・発注・約定の記録は人が行う。実際の取引・保有・判断は git 管理外の data/live/ に記録し、研究との差を後から検証できる。

次は Phase 0 の残り（戦略・口座・毎月の金額・注文の上限を決めて config/live.local.toml に書く）を終えて、少額の実運用を始める。以降は、実運用の記録と研究結果の比較を定期的に行い、必要に応じて研究テーマを更新する。
